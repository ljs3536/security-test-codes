# 1. 원격 코드 실행 (RCE) 및 직렬화 취약점
PyYAML==3.12

# 2. SQL 인젝션 및 경로 탐색 (Path Traversal) 취약점
Django==2.0.7

# 3. 중요 정보 및 인증 정보 노출 취약점
requests==2.19.1

# 4. 버퍼 오버플로우 및 서비스 거부(DoS) 취약점
Pillow==8.1.0

# 5. 샌드박스 우회 및 크로스 사이트 스크립팅(XSS) 취약점
Jinja2==2.10

# 6. JWT 서명 검증 우회 취약점
PyJWT==1.7.1

# 7. CRLF 인젝션 취약점
urllib3==1.25.7

# 8. XML 외부 엔티티(XXE) 및 메모리 누수
lxml==4.6.1