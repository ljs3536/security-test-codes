# # --- 1. 기존 취약점 (CVE 매칭 복구 확인용) ---
# Django==4.1.0           # CVE-2023-31047 (XSS)
# requests==2.28.0        # CVE-2023-32681 (Leakage)
# PyYAML==5.3.1           # CVE-2020-14343 (RCE)
# urllib3==1.26.4         # CVE-2021-33503 (ReDoS)
# cryptography==3.3.2     # CVE-2023-23931
# jinja2==2.11.2          # CVE-2020-28493

# # --- 2. [신규] AI / 데이터 분석 라이브러리 취약점 ---
# numpy==1.21.0           # CVE-2021-33430 등 메모리 손상 취약점 존재
# scikit-learn==0.23.1    # 구버전, 하위 의존성(Joblib 등)의 취약점 연쇄 작용 테스트

# # --- 3. [신규] 라이선스 리스크 (AGPL) 감지 테스트 ---
# PyMuPDF==1.22.0         # 아주 강력한 '전염성'을 가진 AGPL-3.0 라이선스 적용 패키지

# # --- 4. [신규] 복잡한 버전 표기법 (파서 한계 테스트) ---
# Werkzeug>=1.0.0,<2.0.0  # 다중 범위 지정 파싱 테스트
# Flask~=1.1.2            # 호환 릴리스(Tilde, ~) 표기법 파싱 테스트

# # --- 5. [신규] 방치된(Deprecated/Unmaintained) 오픈소스 ---
# django-rest-swagger==2.2.0 # 수년간 업데이트가 중단된 버려진 프로젝트

# # --- 6. 버전 미고정 (기존 유지) ---
# pandas>=1.5.0
# python-dotenv           
# PyJWT==2.8.0